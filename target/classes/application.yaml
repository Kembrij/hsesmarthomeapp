spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_home_app_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: admin
      password: admin

  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    show-sql: true